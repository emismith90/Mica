@model TicketAddEditViewModel
@{ 
    var effortOperations = Model.EffortOperations;
}
<div class="create-new--btn">
    <a title="Xóa" class="js-add-cli" href="javascript:void(0);"
       tce-target-tbl="#effort-tbl"><i class="glyphicon glyphicon-plus"></i> Công</a>
</div>
<div>
    <table id="effort-tbl" class="table table-striped table-hover allow-delete ticket-effort-op-detail">
        @if (effortOperations != null && effortOperations.Any())
        {
            var dummyDetail = effortOperations.FirstOrDefault();
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(m => dummyDetail.EffortName)</th>
                    <th>@Html.DisplayNameFor(m => dummyDetail.Quantity)</th>
                    <th>@Html.DisplayNameFor(m => dummyDetail.EffortUnitPrice)</th>
                    <th>@Html.DisplayNameFor(m => dummyDetail.EffortPrice)</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @for (var index = 0; index < effortOperations.Count; index++)
                {
                    <tr>
                        <td>
                            <select class="form-control" name="EffortId" required >
                                <option value="">Chọn loại công...</option>
                                @if (Model.Efforts != null && Model.Efforts.Any())
                                {
                                    foreach (var effort in Model.Efforts)
                                    {
                                        if (effort.Value == effortOperations[index].EffortId.ToString())
                                        {
                                            <option selected value="@effort.Value">@effort.Text</option>
                                        }
                                        else
                                        {
                                            <option value="@effort.Value">@effort.Text</option>
                                        }
                                    }
                                }
                            </select>
                        </td>
                        <td>
                            <input class="form-control" type="number" name="Quantity" required min="0" max="999999" 
                                   tce-default-value="@effortOperations[index].Quantity"
                                   value="@effortOperations[index].Quantity"/>
                        </td>
                        <td>
                            <input class="form-control" readonly
                                   tce-default-value="@effortOperations[index].EffortUnitPrice"
                                   value="@effortOperations[index].EffortUnitPrice.ToString("N1")"/>
                        </td>
                        <td>
                            <input class="form-control" readonly
                                   tce-default-value="@effortOperations[index].EffortPrice"
                                   value="@effortOperations[index].EffortPrice.ToString("N1")" />
                        </td>
                        <td>
                            <a title="Xóa" class="js-delete-cli"
                               href="javascript:void(0);">
                               <i class="glyphicon glyphicon-remove"></i>
                            </a>
                        </td>
                    </tr>
                }

                <tr>
                    <td></td>
                    <td></td>
                    <td>Tổng cộng</td>
                    <td>0.0</td>
                    <td></td>
                </tr>

            </tbody>
        }
    </table>
</div>